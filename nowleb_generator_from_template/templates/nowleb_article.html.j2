<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>{{ title }} | Nowleb</title>
  <meta name="description" content="{{ summary[:160] }}"/>
  <link rel="icon" href="https://nowleb.com/assets/images/favicon.png" type="image/png"/>
  <style>
    :root{ --accent:#d32030; --ink:#111; --muted:#666; }
    body{font-family:system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif;margin:0;color:var(--ink);}
    header{position:sticky;top:0;background:#fff;border-bottom:1px solid #eee;padding:12px 16px;z-index:10}
    .brand{display:flex;align-items:baseline;gap:10px}
    .tag{color:var(--muted);font-size:13px}
    main{padding:16px;max-width:980px;margin:0 auto}
    .meta{color:var(--muted);font-size:14px;margin:6px 0 16px}
    .hero{border-radius:12px;overflow:hidden;margin:8px 0 4px;background:#f6f6f6}
    .hero img{width:100%;display:block}
    .summary{font-size:18px;line-height:1.5}
    .bar{height:4px;background:var(--accent);margin:18px 0}
    .share a{border:1px solid #ddd;border-radius:8px;padding:6px 10px;text-decoration:none;color:inherit;margin-right:8px}
    footer{margin-top:40px;padding:20px;border-top:1px solid #eee;color:var(--muted);font-size:13px}
  </style>
</head>
<body>
  <header>
    <div class="brand">
      <h1 style="margin:0">Nowleb</h1>
      <span class="tag">Entertainment · Food · Sports · Events · Music · Fashion</span>
    </div>
  </header>

  <main>
    <div class="bar"></div>
    <a href="/news/" style="color:var(--muted);text-decoration:none;">← Back to News / {{ category }}</a>
    <h2 style="margin:8px 0 8px">{{ title }}</h2>

    <div class="meta">
      <strong>{{ source }}</strong> • {{ date_str }} • {{ read_minutes }} min read
    </div>

    {% if hero_image %}
    <div class="hero"><img src="{{ hero_image }}" alt=""></div>
    {% endif %}

    <p class="summary"><strong>{{ summary }}</strong></p>
    <p><a href="{{ original_url }}" target="_blank" rel="noopener">Read original source →</a></p>

    <div class="share" style="margin-top:16px">
      <a href="https://t.me/share/url?url={{ original_url }}&text={{ title | urlencode }}" target="_blank">Share on Telegram</a>
      <a href="https://twitter.com/intent/tweet?text={{ title | urlencode }}&url={{ original_url | urlencode }}" target="_blank">Share on X</a>
      <a href="#" onclick="navigator.clipboard.writeText('{{ original_url }}');return false;">Copy link</a>
    </div>
  </main>

  <footer>© Nowleb</footer>
</body>
</html>
